services:
  video-show:
    build: .
    container_name: video-show
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/video_show.db
      FRONTEND_DIR: /app/frontend/dist
      CORS_ORIGINS: ${CORS_ORIGINS:-https://wikiroco.com,http://localhost:8002}
    ports:
      - "${APP_PORT:-8002}:8002"
    volumes:
      - ./data:/app/data
